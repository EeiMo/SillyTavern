{
  "name": "sillytavern-android",
  "version": "0.1.0",
  "description": "Android WebView wrapper for SillyTavern (AGPL-3.0-only)",
  "license": "AGPL-3.0-only",
  "private": true,
  "scripts": {
    "build": "vite build",
    "sync": "npm run build && npx cap sync android",
    "add:android": "npm run build && npx cap add android",
    "open:android": "npm run build && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/preferences": "^7.0.0",
    "@capacitor/status-bar": "^7.0.6"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "vite": "^5.4.0"
  }
}
